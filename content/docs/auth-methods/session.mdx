---
title: 🌐 Сессионная авторизация
description: Настройки автоматической авторизации по IP-адресу в BaronessAuth
---

# Сессионная авторизация

**Путь конфига:** `content/BaronessAuth/config/methods/session.yml`

Автоматическая авторизация по IP-адресу для повторных входов в течение определенного времени.

## 📋 Описание

Сессионная авторизация — это метод аутентификации, который автоматически пропускает игроков, если они недавно входили с того же IP-адреса. Это обеспечивает удобство для постоянных игроков, избавляя их от необходимости каждый раз вводить пароль.

## ⚙️ Основные настройки

### `enabled`
Включает или отключает сессионную авторизацию.

```yml
enabled: true  # Включить сессионную авторизацию
enabled: false # Отключить сессионную авторизацию
```

### `display-name`
Человеческое название метода, которое показывается игрокам.

```yml
display-name: 'сессию'
```

## ⏰ Настройки времени

### Максимальная длительность сессии

```yml
max-time: '12 ч'
```

**Параметры:**
- `max-time` - максимальное время, в течение которого сессия считается действительной
- Поддерживаемые форматы: `сек`, `мин`, `ч`, `дн`

### Примеры времени

```yml
max-time: '30 мин'    # 30 минут
max-time: '2 ч'        # 2 часа
max-time: '1 дн'       # 1 день
max-time: '7 дн'       # 7 дней
```

## 🔒 Настройки безопасности

### Отключение по правам

```yml
disable-by-permission:
  enabled: false
  permission: 'BaronessAuth.method.session.disable'
```

**Параметры:**
- `enabled` - включить ли возможность отключения сессии по правам
- `permission` - право для отключения сессии

### Дополнительные проверки

```yml
security:
  # Проверять ли изменение IP
  check-ip-change: true
  
  # Разрешить ли сессии с динамических IP
  allow-dynamic-ip: false
  
  # Максимальное количество сессий на IP
  max-sessions-per-ip: 5
```

## 💬 Настройки сообщений

### Сообщение в чат

```yml
message:
  enabled: false
  content: '<green>Ваша сессия сохранена, аутентификация не требуется.'
```

**Параметры:**
- `enabled` - показывать ли сообщение в чат при входе через сессию
- `content` - текст сообщения

### Тайтл на весь экран

```yml
title:
  enabled: true
  title: '<green>С возвращением!'
  subtitle: '<green>Ваша сессия сохранена!'
  duration-fade-in: '500 мсек'
  duration-stay: '3 сек 500 мсек'
  duration-fade-out: '1 сек'
```

**Параметры:**
- `enabled` - показывать ли тайтл при входе через сессию
- `title` - заголовок тайтла
- `subtitle` - подзаголовок тайтла
- `duration-fade-in` - время появления
- `duration-stay` - время отображения
- `duration-fade-out` - время исчезновения

## 🔧 Дополнительные настройки

### Команда выхода

```yml
logout:
  # Команда для выхода из сессии
  command: '/logout'
  
  # Сообщение при выходе
  message: '<yellow>Вы вышли из сессии. При следующем входе потребуется аутентификация.'
  
  # Время действия команды
  cooldown: '5 сек'
```

### Очистка сессий

```yml
cleanup:
  # Автоматическая очистка устаревших сессий
  enabled: true
  
  # Интервал очистки
  interval: '1 ч'
  
  # Логирование очистки
  log-cleanup: false
```

## 📊 Статистика

### Отслеживание

```yml
statistics:
  # Включить ли отслеживание статистики
  enabled: true
  
  # Сохранять ли логи сессий
  log-sessions: true
  
  # Показывать ли статистику администраторам
  show-to-admins: true
```

## 📝 Пример полного конфига

```yml
enabled: true
display-name: 'сессию'

# Максимальная длительность сессии
max-time: '12 ч'

# Отключение сессии для владельцев определённого права
disable-by-permission:
  enabled: false
  permission: 'BaronessAuth.method.session.disable'

# Сообщение в чат при успешном входе через этот способ
message:
  enabled: false
  content: '<green>Ваша сессия сохранена, аутентификация не требуется.'

# Сообщение на весь экран при успешном входе через этот способ
title:
  enabled: true
  title: '<green>С возвращением!'
  subtitle: '<green>Ваша сессия сохранена!'
  duration-fade-in: '500 мсек'
  duration-stay: '3 сек 500 мсек'
  duration-fade-out: '1 сек'

# Дополнительные настройки безопасности
security:
  check-ip-change: true
  allow-dynamic-ip: false
  max-sessions-per-ip: 5

# Настройки команды выхода
logout:
  command: '/logout'
  message: '<yellow>Вы вышли из сессии. При следующем входе потребуется аутентификация.'
  cooldown: '5 сек'

# Настройки очистки
cleanup:
  enabled: true
  interval: '1 ч'
  log-cleanup: false

# Настройки статистики
statistics:
  enabled: true
  log-sessions: true
  show-to-admins: true
```

## 🔧 Как это работает

### Принцип работы

1. **Первый вход**: Игрок проходит обычную аутентификацию (пароль, 2FA и т.д.)
2. **Сохранение сессии**: После успешной аутентификации сохраняется сессия с IP-адресом
3. **Повторный вход**: При следующем входе с того же IP проверяется наличие активной сессии
4. **Автоматический вход**: Если сессия активна, игрок автоматически пропускается
5. **Истечение сессии**: По истечении времени сессия удаляется

### Проверка IP

```yml
ip-check:
  # Проверять ли изменение IP
  enabled: true
  
  # Разрешить ли небольшие изменения IP
  allow-minor-changes: false
  
  # Максимальное количество изменений IP
  max-ip-changes: 0
```

## ⚠️ Безопасность

### Риски

- **Прокси**: При использовании прокси другой человек может получить тот же IP
- **Динамические IP**: При смене IP сессия может быть потеряна
- **Общие сети**: В общественных сетях IP может быть общим

### Рекомендации

```yml
recommendations:
  # Использовать короткое время сессии для безопасности
  short-session-time: '2 ч'
  
  # Отключать для администраторов
  disable-for-admins: true
  
  # Логировать подозрительную активность
  log-suspicious-activity: true
```

## 🔄 Совместимость

### С другими методами

- **Парольная авторизация**: Работает как основной метод для новых игроков
- **Telegram 2FA**: Можно использовать для дополнительной защиты
- **Premium авторизация**: Работает для лицензионных игроков

### Приоритет методов

1. **Сессионная авторизация** - проверяется первой
2. **Premium авторизация** - для лицензионных игроков
3. **Telegram 2FA** - двухфакторная аутентификация
4. **Парольная авторизация** - основной метод

## 📱 Управление сессиями

### Команды администратора

```yml
admin-commands:
  # Просмотр активных сессий
  sessions:
    command: '/auth sessions'
    permission: 'BaronessAuth.admin.sessions'
  
  # Очистка сессий игрока
  clear-sessions:
    command: '/auth clearsessions <player>'
    permission: 'BaronessAuth.admin.clearsessions'
  
  # Очистка всех сессий
  clear-all-sessions:
    command: '/auth clearallsessions'
    permission: 'BaronessAuth.admin.clearallsessions'
```

### Команды игрока

```yml
player-commands:
  # Выход из сессии
  logout:
    command: '/logout'
    aliases:
    - '/logoff'
    - '/signout'
  
  # Статус сессии
  session-status:
    command: '/session'
    aliases:
    - '/sessionstatus'
```

## ⚠️ Важные замечания

- **Сессионная авторизация** основана на IP-адресе и может быть небезопасна при использовании прокси
- Рекомендуется использовать короткое время сессии для повышения безопасности
- Не забудьте настроить отключение сессии для администраторов
- Регулярно проверяйте логи для выявления подозрительной активности
- Используйте в сочетании с другими методами для максимальной безопасности

## 🔧 Настройка для разных сценариев

### Домашние серверы

```yml
# Для домашних серверов с постоянными игроками
max-time: '24 ч'
security:
  allow-dynamic-ip: true
  max-sessions-per-ip: 10
```

### Публичные серверы

```yml
# Для публичных серверов с повышенной безопасностью
max-time: '2 ч'
security:
  allow-dynamic-ip: false
  max-sessions-per-ip: 3
  check-ip-change: true
```

### Серверы с прокси

```yml
# Для серверов с прокси (не рекомендуется)
max-time: '30 мин'
security:
  allow-dynamic-ip: false
  max-sessions-per-ip: 1
  check-ip-change: true
``` 