---
title: link.yml - Команда привязки аккаунта
description: Настройки команды привязки аккаунта в BaronessAuth
---

# Файл `link.yml`

**Путь:** `content/BaronessAuth/config/commands/link.yml`

Команда `/link [сервис]`. Она позволяет игроку привязать свой аккаунт к какому-то из включённых сервисов (VK, Telegram, Discord и т.д.).

Если у вас не включены никакие сервисы, команда автоматически исчезнет.

## 📋 Содержимое файла

```yml
# ┌────────────────────────────────
# │ Команда /link [сервис].
# │ Она позволяет игроку привязать свой аккаунт к какому-то из включённых сервисов (VK, Telegram, Discord и т.д.).
# │ Если у вас не включены никакие сервисы, команда автоматически исчезнет.
# └────────────────────────────────
aliases:
- 'link'
cooldown: '1 сек 200 мсек'
permissions: []
arguments:
  service: 'сервис'
messages:
  available-services: |-
    <yellow>Доступные сервисы: <white><services></white></yellow>
    <yellow>Введите <white>/link [сервис]</white> для привязки.</yellow>
```

## 🔧 Основные секции

### Aliases
Алиасы команды:
- **link** - основная команда

### Cooldown
Задержка между использованиями:
- **cooldown** - время задержки (1 сек 200 мсек)

### Permissions
Права доступа:
- **permissions** - список прав (пустой - доступно всем)

### Arguments
Аргументы команды:
- **service** - название сервиса для привязки

### Messages
Сообщения команды:
- **available-services** - список доступных сервисов

## 🔐 Как это работает

1. **Игрок вводит** `/link [сервис]`
2. **Плагин проверяет** доступность сервиса
3. **Генерируется код** для привязки
4. **Игрок использует код** в соответствующем сервисе
5. **Аккаунт привязывается** к сервису

## 🔐 Доступные сервисы

- **VK** - привязка к ВКонтакте
- **Telegram** - привязка к Telegram
- **Discord** - привязка к Discord

## 🔐 Рекомендуемые настройки

Для большинства серверов безопасные настройки:

```yml
aliases:
- 'link'
cooldown: '1 сек'
permissions: []

arguments:
  service: 'сервис'

messages:
  available-services: |-
    <yellow>Доступные сервисы: <white><services></white></yellow>
    <yellow>Введите <white>/link [сервис]</white> для привязки.</yellow>
```

## ⚠️ Важно

- Команда доступна только при включенных сервисах привязки
- Каждый сервис требует отдельной настройки в соответствующих конфигах
- Привязка позволяет получать уведомления и использовать 2FA
- Игроки могут отвязать аккаунт через интерфейс сервиса 