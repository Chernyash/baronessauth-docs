---
title: unregister.yml - Команда удаления аккаунта
description: Настройки команды удаления аккаунта в BaronessAuth
---

# Файл `unregister.yml`

**Путь:** `content/BaronessAuth/config/commands/unregister.yml`

Команда `/unregister [текущий пароль]`. Она позволяет игроку удалить свой аккаунт.

Это удаляет все данные BaronessAuth о нём (пароль, привязки и так далее), но его игровой прогресс останется.

## 📋 Содержимое файла

```yml
# ┌────────────────────────────────
# │ Команда /unregister [текущий пароль].
# │ Она позволяет игроку удалить свой аккаунт.
# │ Это удаляет все данные BaronessAuth о нём (пароль, привязки и так далее), но его игровой прогресс останется.
# └────────────────────────────────
aliases:
- 'unregister'
cooldown: '5 сек'
permissions:
- 'BaronessAuth.command.unregister'
arguments:
  password: 'ваш пароль'
messages:
  success: '<green>Ваша регистрация удалена.'
```

## 🔧 Основные секции

### Aliases
Алиасы команды:
- **unregister** - основная команда

### Cooldown
Задержка между использованиями:
- **cooldown** - время задержки (5 сек)

### Permissions
Права доступа:
- **permissions** - список прав (`BaronessAuth.command.unregister`)

### Arguments
Аргументы команды:
- **password** - текущий пароль для подтверждения

### Messages
Сообщения команды:
- **success** - сообщение об успешном удалении регистрации

## 🔐 Как это работает

1. **Игрок вводит** `/unregister [текущий пароль]`
2. **Плагин проверяет** правильность пароля
3. **Плагин удаляет** все данные BaronessAuth об игроке
4. **Игровой прогресс** остается нетронутым
5. **Игрок получает** подтверждение об удалении

## 🔐 Рекомендуемые настройки

Для большинства серверов безопасные настройки:

```yml
aliases:
- 'unregister'
cooldown: '5 сек'
permissions:
- 'BaronessAuth.command.unregister'

arguments:
  password: 'ваш пароль'

messages:
  success: '<green>Ваша регистрация удалена.'
```

## ⚠️ Важно

- Команда требует ввода текущего пароля для подтверждения
- Удаляются только данные BaronessAuth, игровой прогресс сохраняется
- Требует специальное право `BaronessAuth.command.unregister`
- Необратимая операция - данные нельзя восстановить
- Полезно для полного удаления аккаунта 