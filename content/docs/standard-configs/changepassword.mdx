---
title: changepassword.yml - Команда смены пароля
description: Настройки команды смены пароля в BaronessAuth
---

# Файл `changepassword.yml`

**Путь:** `content/BaronessAuth/config/commands/changepassword.yml`

Команда `/changepassword [старый пароль] [новый пароль]`. Она позволяет игроку сменить собственный пароль.

## 📋 Содержимое файла

```yml
# ┌────────────────────────────────
# │ Команда /changepassword [старый пароль] [новый пароль].
# │ Она позволяет игроку сменить собственный пароль.
# └────────────────────────────────
aliases:
- 'changepassword'
cooldown: '4 сек'
permissions: []
arguments:
  old-password: 'старый пароль'
  new-password: 'новый пароль'
messages:
  success: '<green>Ваш пароль был изменен.'
```

## 🔧 Основные секции

### Aliases
Алиасы команды:
- **changepassword** - основная команда

### Cooldown
Задержка между использованиями:
- **cooldown** - время задержки (4 сек)

### Permissions
Права доступа:
- **permissions** - список прав (пустой - доступно всем)

### Arguments
Аргументы команды:
- **old-password** - старый пароль для подтверждения
- **new-password** - новый пароль

### Messages
Сообщения команды:
- **success** - сообщение об успешной смене пароля

## 🔐 Как это работает

1. **Игрок вводит** `/changepassword [старый пароль] [новый пароль]`
2. **Плагин проверяет** правильность старого пароля
3. **Плагин проверяет** соответствие нового пароля требованиям
4. **Пароль обновляется** в базе данных
5. **Игрок получает** подтверждение об успешной смене

## 🔐 Рекомендуемые настройки

Для большинства серверов безопасные настройки:

```yml
aliases:
- 'changepassword'
cooldown: '4 сек'
permissions: []

arguments:
  old-password: 'старый пароль'
  new-password: 'новый пароль'

messages:
  success: '<green>Ваш пароль был изменен.'
```

## ⚠️ Важно

- Команда требует ввода старого пароля для подтверждения
- Новый пароль должен соответствовать требованиям безопасности
- Задержка предотвращает частую смену паролей
- Полезно для безопасности при подозрении на компрометацию 