---
title: logout.yml - Команда выхода
description: Настройки команды выхода в BaronessAuth
---

# Файл `logout.yml`

**Путь:** `content/BaronessAuth/config/commands/logout.yml`

Команда `/logout`. Она позволяет сбросить свою сессию, то есть отключить автоматический вход при следующем подключении.

У этого действия есть побочный эффект - последняя дата входа игрока в `/auth stats nick` исчезает.

## 📋 Содержимое файла

```yml
# ┌────────────────────────────────
# │ Команда /logout.
# │ Она позволяет сбросить свою сессию,
# │ то есть отключить автоматический вход при следующем подключении.
# │ 
# │ У этого действия есть побочный эффект - последняя дата входа игрока в /auth stats nick исчезает.
# └────────────────────────────────
aliases:
- 'logout'
cooldown: '10 сек'
permissions: []
messages:
  success: '<green>Ваша сессия сброшена.'
```

## 🔧 Основные секции

### Aliases
Алиасы команды:
- **logout** - основная команда

### Cooldown
Задержка между использованиями:
- **cooldown** - время задержки (10 сек)

### Permissions
Права доступа:
- **permissions** - список прав (пустой - доступно всем)

### Messages
Сообщения команды:
- **success** - сообщение об успешном сбросе сессии

## 🔐 Как это работает

1. **Игрок вводит** `/logout`
2. **Плагин сбрасывает** сессию игрока
3. **При следующем входе** игрок должен будет пройти аутентификацию
4. **Статистика входа** обновляется

## 🔐 Рекомендуемые настройки

Для большинства серверов безопасные настройки:

```yml
aliases:
- 'logout'
cooldown: '10 сек'
permissions: []

messages:
  success: '<green>Ваша сессия сброшена.'
```

## ⚠️ Важно

- Команда сбрасывает сессию игрока
- При следующем входе потребуется аутентификация
- Статистика последнего входа обновляется
- Полезно для безопасности при использовании общего компьютера 